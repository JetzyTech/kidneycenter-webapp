export const ROUTES = {
	// ----------------- Auth Routes -----------------
	auth: `/auth`,
	// ----------------- App Routes -----------------
	app: `/app`,
	payment: "/app/payments/:slug",
	confirmPayment: "/app/payments/confirm",
}
